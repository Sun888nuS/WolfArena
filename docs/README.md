# docs

项目文档目录，记录阶段计划、部署说明和重要架构背景。

## 这里负责什么

- `phase-*.md` 记录不同阶段的实现目标、完成情况和设计约束。
- `deployment.md` 记录部署相关说明。

## 常见修改入口

- 改项目路线、阶段目标、架构说明：新增或修改这里的文档。
- 改部署步骤、环境变量、容器运行方式：`deployment.md`。

## 边界说明

这里是文档，不是运行时代码。实现变更落在 `backend/` 或 `frontend/` 后，关键决策再同步到这里。
