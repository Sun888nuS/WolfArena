# 修改记录：修复前端身份漂移显示

## 修改时间

2026-06-27 16:54

## 修改范围

- `frontend/src/features/game/GamePage.tsx`
- `backend/tests/test_game_api.py`
- `修改记录/20260627_1654_修复前端身份漂移显示.md`

## 修改内容

- 前端新增当前活动对局 `game_id` 过滤，避免旧 WebSocket、旧自动推进请求或旧提交请求晚到后覆盖当前对局快照。
- 前端按单局锁定真人玩家首次可见身份和阵营；同一局后续快照如果出现漂移，展示层沿用锁定身份。
- 首次进入页面恢复旧局时，如果用户已经开始或切换到另一局，则丢弃晚到的恢复快照。
- 新增后端回归测试，覆盖“真人身份从开局推进到天亮后保持不变”的规则不变量。

## 验证方式

- `backend/` 下运行：`.\.venv\Scripts\python.exe -m pytest tests\test_game_api.py -q`
  - 结果：`11 passed, 1 warning`
- `frontend/` 下运行：`npm.cmd run build`
  - 结果：TypeScript 检查和 Vite 构建通过。

## 备注

- 本次排查中，后端规则状态从夜晚推进到白天未复现身份改变；更符合前端异步快照晚到覆盖导致的显示漂移。
