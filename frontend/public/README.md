# frontend/public

Vite 静态资源目录。

## 这里负责什么

放不需要经过打包处理、会按原路径复制到构建产物中的公开静态文件，例如 favicon、固定图片、robots.txt 等。

## 常见修改入口

- 添加浏览器直接访问的静态资源：放在这里。
- 页面组件里通过普通 URL 引用这些资源时，路径通常从 `/` 开始。

## 边界说明

业务组件、样式和 TypeScript 代码不要放在这里。需要被 import、压缩或参与构建的资源，优先放在 `frontend/src/` 对应功能目录。
