# data

本地运行数据目录。

## 这里负责什么

当前目录包含 sqlite 数据文件，属于本地运行或历史实验数据。当前主代码路径说明里强调：游戏会话和 LangGraph checkpointer 主要保存在后端进程内，重启后不会恢复旧局。

## 常见修改入口

- 一般不需要手动改这里。
- 如果未来实现持久化会话或持久化 checkpoint，可以把数据库文件、迁移产物或本地测试数据放在这里。

## 边界说明

不要在这里写业务代码。持久化逻辑应放在 `backend/app/db/` 或相关应用模块中。
